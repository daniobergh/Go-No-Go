# Go-No-Go

This is a Go/NoGo protocol task. The task has 50 trials. 40 trials are 'go' and 10 are 'no go'. During 'go' trials, the participant should press the key as fast as possible. During 'no go' trials, the participant should not press a key. 

#The code sets up the dialog box and disables the mouse.
#The code reads in the necessary CSV file and then draws and displays the instructions for the task.
#The keyboard is initialized, looking for the SPACE key.
#The code then pulls the food images and prepares the red and green circles for 'go' and 'no go', along with the 'no' versus 'no go' trials
#The trial results will finally be saved in a new CSV file
